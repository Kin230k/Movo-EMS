CREATE TABLE CRITERIA_RESULTS (
    criterionResultId UUID PRIMARY KEY,
    answerId UUID NOT NULL REFERENCES ANSWERS(answerId) ON DELETE CASCADE ON UPDATE CASCADE,
    criterionId UUID NOT NULL REFERENCES CRITERIA(criterionId) ON DELETE CASCADE ON UPDATE CASCADE,
    passed BOOLEAN NOT NULL,
    evaluatedAt TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);