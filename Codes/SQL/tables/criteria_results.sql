CREATE TABLE CRITERIA_RESULTS (
    criterionResultId UUID PRIMARY KEY,
    answerId UUID NOT NULL REFERENCES ANSWERS(answerId),
    criterionId UUID NOT NULL REFERENCES CRITERIA(criterionId),
    passed BOOLEAN NOT NULL,
    evaluatedAt TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);